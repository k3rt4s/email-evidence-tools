# ============================================================
# crosier_bowker_complaint - Python dependencies
# ============================================================
# This project runs in its own isolated venv:
#   D:\venvs\crosier_bowker_complaint\
# Its bootstrap loads ONLY this file (not the ai-toolkit master).
# ============================================================

pandas>=2.2.2
python-dotenv>=1.0.0
